spring:
  datasource:
    url: jdbc:postgresql://db:5432/kotlin
    username: root
    password: root
    driverClassName: org.postgresql.Driver
  jpa:
    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate.ddl-auto: update